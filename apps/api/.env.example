# JWT Secret Key (최소 32자 이상의 랜덤 문자열, 배포 간 동일 값 유지)
# 생성 방법: openssl rand -base64 32
JWT_SECRET=your-strong-secret-key-minimum-32-characters-long

# 로컬 DynamoDB 엔드포인트
DYNAMODB_ENDPOINT=http://localhost:8000

# 환경 설정
IS_OFFLINE=true
SLS_STAGE=local
