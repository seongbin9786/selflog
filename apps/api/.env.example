# JWT Secret Key (최소 32자 이상의 랜덤 문자열)
# 생성 방법: openssl rand -base64 32
JWT_SECRET=your-strong-secret-key-minimum-32-characters-long

# DynamoDB 테이블 이름 (로컬 개발용)
DYNAMODB_TABLE_USERS=my-time-users-local
DYNAMODB_TABLE=my-time-logs-local

# 로컬 DynamoDB 엔드포인트
DYNAMODB_ENDPOINT=http://localhost:8000

# 환경 설정
IS_OFFLINE=true
